apply from: rootProject.file("library.gradle")

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'androidx.fragment:fragment:1.3.6'
    api deps.libphonenumberAndroid
}
